import { NameObjJoinPipe } from './name-obj-join.pipe';

describe('NameObjJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new NameObjJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
